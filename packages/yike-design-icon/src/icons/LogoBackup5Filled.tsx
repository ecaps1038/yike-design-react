/**
 * 🎉🎉🎉 GENERATE BY @yike-design/cli
 * ⚠️⚠️⚠️ DO NOT EDIT IT MANUALLY
 */

import React from 'react';
import type { YiKeIconProps } from '../types';
import YiKeIcon from '../components/YiKeIcon';
import LogoBackup5FilledSVG from './svg/LogoBackup5Filled';

const LogoBackup5Filled = React.forwardRef<HTMLSpanElement, YiKeIconProps>((props, ref) => {
  return (
    <YiKeIcon
      {...props}
      ref={ref}
      name="logo-backup-5-filled"
      icon={LogoBackup5FilledSVG}
    />
  );
});

export default LogoBackup5Filled;
