# Button 按钮

## 按钮类型 type

按钮有三种类型：主按钮、次按钮、线框按钮。主按钮在同一个操作区域建议最多出现一次。


```tsx
import { Button } from '@yike-design/react'

export default () => {
  return (
    <div>
      <Button>主要按钮</Button>
      <Button>次要按钮</Button>
      <Button>线框按钮</Button>
    </div>
  )
}
```

