import { createPage } from '@/utils/doc';

const { generateStaticParams, generateMetadata, Page } = createPage('components');

export { generateStaticParams, generateMetadata };

export default Page;
